import Axios from "@/utils/axios"

const Actions = user => {
  const {token} = user

  return {
    // GET
    getAll: async () => { 

    },
    getById: async id => { 

    },

    // MUTATIONS
    create: async data => { 

    },
    update: async (id, data) => {

    },
    delete: async id => {

    }
  }
}

export default Actions
